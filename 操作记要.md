# FlowBoard 操作记要

## 2026-08-13
### 1. 新增任务评论功能 (Task Comments)
- **数据库表结构更新**：在 `db/schema.ts` 中新增了 `task_comments` 表，用于存储评论内容及关联的任务 ID 和作者 ID。并生成与执行了 Drizzle 迁移脚本。
- **后端 API 新增**：新增了 `app/api/tasks/[id]/comments/route.ts`，提供 GET 与 POST 方法；在发表评论时，排除发言人自己，给任务的相关参与者（开发负责人、测试负责人等）写入系统通知。
- **前端组件开发**：在 `app/dashboard/board` 下新增了 `task-comments.tsx` 任务沟通动态组件。
- **任务详情集成**：修改了 `task-board.tsx`，在任务编辑模态框内部下方集成了新开发的 `TaskComments` 组件。
- **UI 样式调整**：在 `app/globals.css` 中添加了任务评论相关的 CSS 样式。

### 2. 优化分页组件 (Pagination)
- **重构组件布局**：修改了 `app/dashboard/pagination-controls.tsx`，移除了原有多余的“第 X / Y 页”等纯文本描述，并给主容器加上了 `compact` 样式。
- **紧凑型 CSS**：在 `app/globals.css` 中新增了 `.list-pagination.compact` CSS 定义，大幅减小了分页组件的高度 (min-height)、内边距 (padding) 与按钮尺寸，释放了更多屏幕空间。
